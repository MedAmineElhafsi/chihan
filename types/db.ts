/**
 * Generated Supabase database types will live here from Phase 1 onward:
 *
 *   npx supabase gen types typescript --project-id <ref> > types/db.ts
 *
 * Placeholder kept so that imports resolve before the schema exists.
 */
export type Database = Record<string, never>;
